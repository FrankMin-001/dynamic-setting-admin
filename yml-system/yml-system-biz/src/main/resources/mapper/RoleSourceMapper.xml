<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smalldragon.yml.system.mapper.resource.RoleResourceMapper">


<!--    <insert id="insertByBatch">-->
<!--        INSERT INTO  sys_role_resource(id,resource_id,role_id) VALUES-->
<!--        <foreach collection="assignList" item="item" separator=",">-->
<!--            (#{id},#{item.resourceId},#{item.roleId})-->
<!--        </foreach>-->
<!--    </insert>-->

    <delete id="deleteByBatch">
        DELETE FROM sys_role_resource WHERE role_id = #{assignDTO.roleId} AND resource_id IN
        <foreach collection="assignDTO.resourceIds" item="resourceId" open="(" separator="," close=")">
            #{resourceId}
        </foreach>
    </delete>

</mapper>
